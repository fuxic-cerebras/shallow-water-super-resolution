# Verify a gate

Act as Scientific Verifier. Re-run required commands and independently recompute
representative checks rather than trusting logs. Exercise applicable negative tests.
Return blockers/non-blockers, list unverified scope, and mark `verified` only if every
gate requirement passes.
