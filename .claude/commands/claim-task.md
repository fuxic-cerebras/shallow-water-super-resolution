# Claim a task

Read `CLAUDE.md`, `TASKS.md`, and `docs/AGENT_WORKFLOW.md`. Verify dependencies,
role fit, and path ownership. State task ID, gate, files, tests, artifacts, and resources.
Move only that task to `in-progress`; escalate shared-contract changes to the lead.
